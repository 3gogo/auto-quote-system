/**
 * 向量嵌入模块导出
 */

export { 
  EmbeddingService, 
  embeddingService,
  type EmbeddingServiceConfig,
  type EmbeddingResult 
} from './embedding-service';

export { 
  AliasClusterService, 
  aliasClusterService,
  type AliasClusterConfig,
  type ClusterResult,
  type CandidateName 
} from './alias-cluster-service';

